-- oten control


local ScreenGui = Instance.new("ScreenGui")
local Drag = Instance.new("Frame")
local Top = Instance.new("Frame")
local TextLabel = Instance.new("TextLabel")
local Bottom = Instance.new("Frame")
local CashDropped = Instance.new("TextLabel")

--Properties:

ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Drag.Name = "Drag"
Drag.Parent = ScreenGui
Drag.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Drag.BackgroundTransparency = 1.000
Drag.BorderSizePixel = 0
Drag.Position = UDim2.new(0.317555428, 0, 0.262019217, 0)
Drag.Size = UDim2.new(0, 178, 0, 74)

Top.Name = "Top"
Top.Parent = Drag
Top.BackgroundColor3 = Color3.fromRGB(44, 44, 44)
Top.BorderSizePixel = 0
Top.Position = UDim2.new(0.035232693, 0, 0.0959589258, 0)
Top.Size = UDim2.new(0, 165, 0, 20)
Top.ZIndex = 2

TextLabel.Parent = Top
TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
TextLabel.BackgroundTransparency = 1.000
TextLabel.Size = UDim2.new(0, 165, 0, 20)
TextLabel.Font = Enum.Font.GothamBold
TextLabel.Text = "OTEN CONTROL"
TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TextLabel.TextSize = 16.000

Bottom.Name = "Bottom"
Bottom.Parent = Drag
Bottom.BackgroundColor3 = Color3.fromRGB(37, 37, 37)
Bottom.BorderSizePixel = 0
Bottom.Position = UDim2.new(0.0352327153, 0, 0.32568875, 0)
Bottom.Size = UDim2.new(0, 165, 0, 42)

CashDropped.Name = "CashDropped"
CashDropped.Parent = Bottom
CashDropped.BackgroundColor3 = Color3.fromRGB(95, 255, 87)
CashDropped.BackgroundTransparency = 1.000
CashDropped.Position = UDim2.new(-0.000605911016, 0, -0.00705125928, 0)
CashDropped.Size = UDim2.new(0, 165, 0, 42)
CashDropped.Font = Enum.Font.GothamMedium
CashDropped.Text = "$ 1"
CashDropped.TextColor3 = Color3.fromRGB(120, 236, 111)
CashDropped.TextSize = 14.000

-- Scripts:

local function SQMQDTH_fake_script() -- CashDropped.Update 
	local script = Instance.new('Script', CashDropped)

	local MoneyDrops = game:GetService("Workspace").Ignored.Drop
	local amount = 0
	
	
	function comma_value(n) -- credit http://richard.warburton.it
		local left,num,right = string.match(n,'^([^%d]*%d)(%d*)(.-)$')
		return left..(num:reverse():gsub('(%d%d%d)','%1,'):reverse())..right
	end
	
	
	for i,v in pairs(MoneyDrops:GetChildren()) do
		if v:IsA('Part') and v.ClassName ~= 'MeshPart' and v.Name:match('MoneyDrop') and v:FindFirstChild('BillboardGui'):FindFirstChild('TextLabel') then
			money = string.gsub(v.BillboardGui.TextLabel.Text,'[$,]','')
			amount = amount + tonumber(money)
		end
	end
	
	CashDropped.Text = (comma_value('$'..amount))
	
	MoneyDrops.ChildAdded:Connect(function(cash)
		pcall(function()
		money = string.gsub(cash:WaitForChild('BillboardGui'):WaitForChild('TextLabel').Text,'[$,]','')
		amount = amount + tonumber(money)
		CashDropped.Text = ((comma_value('$'..amount)))
		end)
	end)
	
	MoneyDrops.ChildRemoved:Connect(function(cash)
		pcall(function()
		money = string.gsub(cash:WaitForChild('BillboardGui'):WaitForChild('TextLabel').Text,'[$,]','')
		amount = amount - tonumber(money)
		CashDropped.Text = ((comma_value('$'..amount)))
		end)
	end)
	
end
coroutine.wrap(SQMQDTH_fake_script)()
local function CHXOOB_fake_script() -- Drag.drag 
	local script = Instance.new('Script', Drag)

	local a=game:GetService("UserInputService")function drag(b)dragToggle=nil dragSpeed=0.23 dragInput=nil dragStart=nil dragPos=nil function updateInput(a)Delta=a.Position-dragStart Position=UDim2.new(startPos.X.Scale,startPos.X.Offset+Delta.X,startPos.Y.Scale,startPos.Y.Offset+Delta.Y)game:GetService("TweenService"):Create(b,TweenInfo.new(0.25),{Position=Position}):Play()end b.InputBegan:Connect(function(c)if(c.UserInputType==Enum.UserInputType.MouseButton1 or c.UserInputType==Enum.UserInputType.Touch)and a:GetFocusedTextBox()==nil then dragToggle=true dragStart=c.Position startPos=b.Position c.Changed:Connect(function()if c.UserInputState==Enum.UserInputState.End then dragToggle=false end end)end end)b.InputChanged:Connect(function(a)if a.UserInputType==Enum.UserInputType.MouseMovement or a.UserInputType==Enum.UserInputType.Touch then dragInput=a end end)game:GetService("UserInputService").InputChanged:Connect(function(a)if a==dragInput and dragToggle then updateInput(a)end end)end drag(script.Parent)
end
coroutine.wrap(CHXOOB_fake_script)()
