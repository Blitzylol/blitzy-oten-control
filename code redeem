codes = {
    'DHSUPRISE!',
    '2022JUNE',
    'FIREWORKS',
}

for i,v in ipairs(codes) do
    game:GetService("ReplicatedStorage").MainEvent:FireServer('EnterPromoCode', v)
    wait(1.5)
    print(v)
end
